import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        padding: 20,
    },

    text: {
        paddingTop: 20,
        textAlign: 'center',
        fontSize: 25,
        color: 'midnightblue',
    },
})